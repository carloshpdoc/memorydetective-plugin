{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "memorydetective-plugin",
  "description": "iOS leak hunting and performance investigation. Bundles the memorydetective MCP server with a /perf-investigate skill. One-command install for Claude Code.",
  "owner": {
    "name": "Carlos Henrique",
    "url": "https://github.com/carloshpdoc"
  },
  "plugins": [
    {
      "name": "memorydetective",
      "source": "./plugins/memorydetective",
      "description": "iOS leak hunting + performance investigation: 42 MCP tools, 36-pattern cycle classifier with Swift fixTemplate snippets. v1.18 ships analyzeMetricKitPayload (42nd tool, post-mortem production diagnostics from Apple MetricKit `.mxdiagnostic` JSON: crash clusters, hang hotspots with localized-duration parsing, CPU + disk exceptions, cross-tool chain hints) plus audit-close trio (open-enum SupportStatusKind, schemaDiscovery cache for 600-3000ms wall-clock win on summarizeTrace, local-only integration tests against real Apple .trace bundles). v1.17 reliability pass (strtobool env truthy parsing, verifyFix whitelist exact/substring/regex match modes, recordViaInstrumentsApp catches traces saved outside watchDir, fault-tolerant inspectTrace fallback, configurable countAlive noise filter). v1.16 recordViaInstrumentsApp unblocks macOS 26.x trace recording. v1.15 schema-coverage trio (analyzeMemoryFootprint, analyzeEnergyImpact, analyzeLeakTimeline). v1.14 analyzeNetworkActivity + unified supportStatus[]. v1.13 summarizeTrace + /summarize-trace MCP prompt for single-call cross-schema trace synthesis. End-to-end abandoned-memory chain (analyzeAbandonedMemory + countAlive + findRetainers + verifyFix all surface reference-tree data), analyzeHangs auto cross-schema correlation, inspectTrace, diffMemgraphs reference-tree, detectLeaksInXCTest per-test CI gate with HTML reports, cleanupTraces ops tool, single-call build+boot+launch fixing the macOS 26.x leaks --outputGraph regression, replayScenario + captureScenarioState verify-fix loop, compareTracesByPattern for CI gating, /perf-investigate skill. CLI human-output surfaces abandoned-memory suspects by default. Reads .memgraph, .trace, and .mxdiagnostic files; classifies retain cycles; bridges to Swift source via SourceKit-LSP.",
      "category": "development",
      "homepage": "https://github.com/carloshpdoc/memorydetective"
    }
  ]
}
